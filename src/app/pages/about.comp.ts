import { Component, OnInit } from '@angular/core';

@Component({
  // moduleId: module.id,
  selector: 'daln-app',
  template: `
  <h1>{{title}}</h1>
`
})

export class AboutComponent {
    title='About'
}
